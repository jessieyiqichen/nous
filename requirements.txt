# core/ 与 scripts/ 的 Python 依赖（ml/ 训练依赖见 ml/requirements.txt）
# 版本为 2026-07 本机验证过的锁定版本

# 必需：API 调用 + 数据校验
anthropic==0.84.0
pydantic==2.12.5

# stat_detectors.py（统计检测器）
numpy==2.1.3
sentence-transformers==5.2.3

# 可选：hybrid_extractor.py 本地 DistilBERT 推理（缺失时自动 fallback 到 API）
# torch==2.11.0
# transformers==5.2.0

# 可选：scripts/sample_wildchat.py 数据采样
# datasets==4.8.4
